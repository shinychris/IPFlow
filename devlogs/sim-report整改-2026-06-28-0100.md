# 开发日志：仿真测试报告（sim-report）整改

> **日期**: 2026-06-28
> **任务**: 依据 `reports/sim-report.md`（9 个问题）做整改与独立审计（devtasks 委托式）
> **基线**: commit `6a60cfe`（仿真测试中已修的 7 项严重缺陷：#2/#3/#4/#5/#6/#7/#9）

## 背景

本轮 devtasks 依据仿真可用性测试报告 `reports/sim-report.md` 展开。报告对全链路（注册→软著/专利/商标生成→合规→导出→下载）做了 11 步探索，发现 9 个问题（7 严重、1 轻微、1 建议）。报告所述 7 项严重缺陷已在 commit `6a60cfe` 落实为代码。本次按 devtasks 方法论：独立复核 7 项修复→修真实缺口→补回归测试→处理残留项。

## 五色 Agent 分工

| 颜色 | Agent | 任务 | 状态 |
|------|-------|------|------|
| 🔵 蓝 | 后端独立审计 | 对 commit `6a60cfe` 的 7 项修复逐项复核，列出真实缺口 | ✅ 完成 |
| 🟢 绿 | 核心修复 | 修复 export.py / storage_service / uploads 白名单 | ✅ 完成 |
| 🟡 黄 | 辅助修复 | 前端 toast 根因、#1 噪音判定、生产 Nice 种子迁移 | ✅ 完成 |
| 🟠 橙 | 测试加固 | +3 回归测试（路径穿越 / 多包查询） | ✅ 完成 |
| 🔴 红 | 文档 | 撰写本开发日志 | ✅ 完成 |

## 🔵 蓝色独立审计结论（关键）

对 commit `6a60cfe` 的 7 项修复逐项复核源码，**发现 3 个真实缺口（声称已修但实际仍有缺陷）**，其余 4 项确认正确。

| 原 # | 审计结论 | 说明 |
|------|---------|------|
| #1 多包（compliance/job_runner） | ✅ 正确 | 已改为 `scalars().first()` |
| #2 路径穿越安全 | ❌ **缺口** | `file_category` 用户可控→folder→base_path/storage_path 可被 `../` 逃逸 |
| #3 多包导出崩溃 | ❌ **缺口** | `export.py` 直接导出+预览 4 处查询仍用 `scalar_one_or_none()`，多包时 500 |
| #4 None>=int | ✅ 正确 | 已对 None 保护 |
| #5 导出合规参数 | ✅ 正确 | 字段已补全 |
| #6 中文文件名 | ✅ 正确 | RFC 5987 编码 |
| #7 Nice 种子 | ❌ **缺口** | 仅 dev 分支填充，生产（alembic）缺 data migration |

## 🟢 绿色核心修复

- #3 export.py：4 处查询改 `.order_by(created_at.desc()).scalars().first()`
- #2 storage：新增 `_safe_local_path()`（resolve()+parents 比较），覆盖 upload/download/delete/get_presigned_url 4 个 local 分支
- uploads：`ALLOWED_FILE_CATEGORIES` 白名单 + 422 校验

## 🟡 黄色辅助修复

- #8 toast 根因：全站 `<Toaster>` 未挂载（providers.tsx 遗漏），挂载后全站 toast 恢复
- #1 GET /api/v1 404：grep 全前端无发起代码，判定环境噪音，不改
- 生产 Nice 种子：新增 `20260628_seed_nice_classification.py`（down_revision=20260322，幂等，复用 NICE_CLASSES，op.bulk_insert）

## 🟠 橙色测试加固

+3 回归测试：`test_local_backend_rejects_path_traversal`、`test_code_bundle_query_returns_latest_not_crash`、`test_manual_query_returns_latest_not_crash`

## 最终验证

- 后端 `pytest`：✅ **234 passed**（231 baseline + 3 新）
- 前端 `tsc`：✅ **0 错误**
- alembic 链：✅ `20260322 → 20260628 (head)`

## 涉及文件

- 修改：export.py、storage_service.py、uploads.py、providers.tsx、test_sim_fixes.py
- 新增：20260628_seed_nice_classification.py、test_export_multi_bundle.py、本 devlog

## 结论

🔵 蓝色独立审计发现 commit `6a60cfe` 的 3 个真实缺口，🟢🟡 全部修复，🟠 补回归测试。再次印证「声称完成 ≠ 实际无缺陷」。
